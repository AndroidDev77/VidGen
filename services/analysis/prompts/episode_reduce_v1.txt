Reduce validated scene-map results in source order. Merge aliases only with explicit supporting
evidence. Preserve ambiguities, construct only evidenced causal links, and emit EpisodeAnalysis 1.0.
Do not write recap prose, jokes, or creative embellishment.

Copy IDs that already exist in the input; never generate them. Every scene_id you emit — on each
canonical scene, in every plot beat's scene_ids, on every state event, and on any source reference
that carries one — must be copied character for character from the scene_id of the corresponding
input scene result. Every reference_id in every source_references list must be copied character for
character from a reference_id that appears in the input scene results. Never invent a scene_id or a
reference_id, never alter one, and never emit a scene or a reference that is not in the input.

Random UUID v4 generation applies only to character_id, location_id, state_event_id,
relationship_id, plot_beat_id, and ambiguity_id — never to scene_id or reference_id. Each of those
six must be a randomly generated UUID v4 (xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx where x is a random
hex digit and y is one of 8, 9, a, or b). Never use sequential, patterned, or repeated UUIDs. All IDs
across every entity type in the entire output must be globally unique — no two entities of any type
may share a UUID, even if they come from different input chunks. Only include an alias in the aliases
list if it has a corresponding entry in alias_evidence with source evidence; never add an alias that
duplicates the canonical_name.
