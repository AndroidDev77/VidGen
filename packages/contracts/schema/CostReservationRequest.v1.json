{
  "additionalProperties": false,
  "properties": {
    "currency": {
      "title": "Currency",
      "type": "string"
    },
    "estimated_amount": {
      "anyOf": [
        {
          "minimum": 0.0,
          "type": "number"
        },
        {
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
          "type": "string"
        }
      ],
      "title": "Estimated Amount"
    },
    "idempotency_key": {
      "title": "Idempotency Key",
      "type": "string"
    },
    "project_id": {
      "format": "uuid",
      "title": "Project Id",
      "type": "string"
    },
    "provider_attempt_id": {
      "format": "uuid",
      "title": "Provider Attempt Id",
      "type": "string"
    },
    "schema_version": {
      "default": "1.0",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "project_id",
    "provider_attempt_id",
    "idempotency_key",
    "estimated_amount",
    "currency"
  ],
  "title": "CostReservationRequest",
  "type": "object"
}
